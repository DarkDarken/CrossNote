<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.programmer.app_beta">

    <uses-sdk
        android:minSdkVersion="14"
        android:targetSdkVersion="21" />
    android:versionCode="1"
    android:versionName="1.0">
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/badger_logo"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".TrainingAddActivity"/>
        <activity android:name=".TrainingListActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".TrainingListActivity" />
        </activity>
        <activity android:name=".TrainingViewActivity"
            android:label="Training"/>
        <activity
            android:name=".AboutPopUpActivity"
            android:theme="@style/AppTheme.CustomThame" />
        <activity android:name=".AddMotionActivity"></activity>
    </application>

</manifest>